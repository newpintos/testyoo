[build]
  publish = "."
  command = "echo 'Static site, no build required'"

# Cache invalidation - forces fresh build
[context.production]
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
